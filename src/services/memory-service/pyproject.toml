[project]
name = "memory-service"
version = "0.1.0"
description = "Chi muc ky uc: ACL, namespace, tombstone, embedding."
requires-python = ">=3.12"
dependencies = [
  "pydantic>=2.9",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/memory_service"]
