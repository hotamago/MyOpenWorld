{
  "$id": "https://myopenworld.dev/schemas/config/app_config.v1.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "BudgetConfig": {
      "additionalProperties": false,
      "description": "Ngân sách nhận thức (`§20.2`).",
      "properties": {
        "max_calls_per_tick": {
          "default": 8,
          "description": "Số lời gọi tối đa mỗi tick trên toàn thế giới.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "max_concurrent": {
          "default": 4,
          "description": "Số lời gọi song song tối đa.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "ContentConfig": {
      "additionalProperties": false,
      "description": "Content pack.",
      "properties": {
        "packs": {
          "default": [
            "core"
          ],
          "description": "Danh sách pack nạp, **theo thứ tự**. Thứ tự quyết định ghi đè.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "root": {
          "default": "content",
          "description": "Thư mục gốc chứa pack.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "EmbeddingConfig": {
      "additionalProperties": false,
      "description": "Cấu hình sinh vector cho chỉ mục ký ức.\n\n**Số chiều không nằm ở đây** — nó là [`VectorConfig::dimension`]. Hai chỗ khai cùng một con số là hai chỗ để chúng lệch nhau, và khi chúng lệch thì triệu chứng là một chỉ mục im lặng trả về kết quả sai chứ không phải một lỗi.",
      "properties": {
        "api_key_env": {
          "default": "EMBEDDINGS_API_KEY",
          "description": "*Tên biến môi trường** chứa API key. Xem [`LlmConfig::api_key_env`].",
          "type": "string"
        },
        "base_url": {
          "default": "",
          "description": "Gốc API, **không** kèm `/embeddings`.\n\nMáy chủ cục bộ dựng bằng `./mow ai up`: `http://localhost:18080/v1`.",
          "type": "string"
        },
        "batch_size": {
          "default": 32,
          "description": "Số văn bản mỗi lời gọi.",
          "format": "uint",
          "minimum": 0.0,
          "type": "integer"
        },
        "document_prefix": {
          "default": "",
          "description": "Tiền tố cho văn bản đóng vai **tài liệu**. Xem [`EmbeddingConfig::query_prefix`].",
          "type": "string"
        },
        "mode": {
          "allOf": [
            {
              "$ref": "#/definitions/EmbeddingMode"
            }
          ],
          "default": "STUB",
          "description": "Chế độ."
        },
        "model": {
          "default": "",
          "description": "Tên model như máy chủ công bố.",
          "type": "string"
        },
        "provider": {
          "default": "",
          "description": "Nhãn nhà cung cấp, cho log và thông báo lỗi.",
          "type": "string"
        },
        "query_prefix": {
          "default": "",
          "description": "Tiền tố cho văn bản đóng vai **truy vấn**.\n\nModel truy xuất hiện đại được huấn luyện bất đối xứng: câu hỏi mã hóa một kiểu, đoạn văn mã hóa kiểu khác. Để trống nếu máy chủ tự áp tiền tố (`jina-embeddings-v5-*-retrieval` đã gộp adapter cho việc đó).\n\nDùng nhầm không làm hỏng gì ra mặt — nó chỉ làm chất lượng truy xuất tụt vài phần trăm, mãi mãi, và không bài test nào đỏ.",
          "type": "string"
        },
        "send_dimensions": {
          "default": true,
          "description": "Có gửi trường `dimensions` hay không.\n\nBật là đúng với model Matryoshka — `jina-embeddings-v5` cắt được về 32/64/128/256/512/768/1024 chiều. Tắt khi máy chủ từ chối trường này.",
          "type": "boolean"
        },
        "timeout_ms": {
          "default": 30000,
          "description": "Thời gian chờ mỗi lời gọi, mili giây.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "EmbeddingMode": {
      "description": "Chế độ sinh vector.\n\nChỉ hai giá trị, và việc **không** có `RECORD`/`REPLAY` ở đây là có chủ ý. `§P6.3` nói chỉ mục ký ức là thứ **dựng lại được**, không phải nguồn sự thật — nên một bộ ghi embedding không mua thêm gì so với [`EmbeddingMode::Stub`], vốn đã xác định tuyệt đối và không cần mạng.",
      "oneOf": [
        {
          "description": "Băm đặc trưng chạy tại chỗ: xác định, không mạng, không khóa.\n\nCho tương đồng **từ vựng**, không phải ngữ nghĩa. Đủ để toàn bộ đường ống ký ức chạy và replay được trước khi có bất kỳ khóa API nào.",
          "enum": [
            "STUB"
          ],
          "type": "string"
        },
        {
          "description": "Gọi một máy chủ embedding thật.",
          "enum": [
            "LIVE"
          ],
          "type": "string"
        }
      ]
    },
    "LlmConfig": {
      "additionalProperties": false,
      "description": "Cấu hình mô hình ngôn ngữ.",
      "properties": {
        "api_key_env": {
          "default": "OPENROUTER_API_KEY",
          "description": "**Tên biến môi trường** chứa API key — không phải key.\n\nĐây là chỗ dễ hiểu sai nhất trong cả file, nên nói thẳng: giá trị đúng là `OPENROUTER_API_KEY`, không phải `sk-or-v1-...`. File này được commit (`§P10.6`), và [`AppConfig::validate`] từ chối khởi động khi giá trị ở đây trông giống một khóa thật.",
          "type": "string"
        },
        "app_title": {
          "default": "",
          "description": "Tên ứng dụng gửi kèm để ghi công (`OpenRouter`: `X-Title`).",
          "type": "string"
        },
        "app_url": {
          "default": "",
          "description": "URL ứng dụng gửi kèm để ghi công (`OpenRouter`: `HTTP-Referer`).",
          "type": "string"
        },
        "base_url": {
          "default": "",
          "description": "Gốc API, **không** kèm `/chat/completions`.\n\n`OpenRouter`: `https://openrouter.ai/api/v1`.",
          "type": "string"
        },
        "cassette_dir": {
          "default": "llm-cassettes",
          "description": "Thư mục chứa bản ghi cho chế độ `RECORD`/`REPLAY`.",
          "type": "string"
        },
        "cognitive_latency_ticks": {
          "default": 10,
          "description": "**Độ trễ nhận thức cố định `D`, tính bằng tick** (`§20.2.2`).\n\nĐây là trường quan trọng nhất trong cả file config, và lý do nó tồn tại đáng đọc kỹ: một thực thể suy nghĩ ở tick `T` sẽ hành động ở tick `T+D`, **bất kể mô hình trả lời nhanh hay chậm**. Không có nó, một mô hình trả lời trong 500ms và một mô hình trả lời trong 3 giây sẽ tạo ra hai thế giới khác nhau từ cùng một seed, và replay trở thành vô nghĩa.\n\nMuốn nhân vật phản ứng nhanh hơn thì tăng `cognition_rate` của nó — đó là thuộc tính của thế giới. Đường truyền nhanh không phải là một thuộc tính của thế giới.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "max_output_tokens": {
          "default": 1024,
          "description": "Trần token đầu ra mỗi lời gọi.\n\nQuá nhỏ thì mô hình bị cắt giữa chừng và trả về một chuỗi rỗng kèm `finish_reason: \"length\"` — thứ trông y hệt \"mô hình không có gì để nói\", và dẫn tới một lần fallback không ai giải thích được.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "mode": {
          "allOf": [
            {
              "$ref": "#/definitions/LlmMode"
            }
          ],
          "default": "STUB",
          "description": "Chế độ."
        },
        "model": {
          "default": "",
          "description": "Mô hình mặc định, ví dụ `deepseek/deepseek-v4-flash-0731`.",
          "type": "string"
        },
        "provider": {
          "default": "",
          "description": "Nhà cung cấp, ví dụ `openrouter`, `openai`.\n\nChỉ là nhãn cho thông báo lỗi và cho log. Thứ thật sự quyết định gọi đi đâu là [`LlmConfig::base_url`] — mọi endpoint theo lược đồ `OpenAI` đều dùng chung một client, nên khóa vào tên nhà cung cấp không mua được gì.",
          "type": "string"
        },
        "temperature_milli": {
          "default": 0,
          "description": "Nhiệt độ lấy mẫu, **tính theo phần nghìn** (`0` = tham lam).\n\nSố nguyên chứ không phải số thực, và đó không phải sự cầu kỳ: cấu hình ảnh hưởng mô phỏng phải ghi được vào event log (`§8.4`), và một `f32` trong event log là một giá trị có thể tuần tự hóa khác nhau giữa hai bản build (`§P10.2.1`).\n\nMặc định 0 cũng là một phát biểu: sự đa dạng của thế giới phải đến từ seed, không từ bộ lấy mẫu — vì đa dạng của bộ lấy mẫu không phát lại được.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "timeout_ms": {
          "default": 30000,
          "description": "Thời gian chờ mỗi lời gọi, tính bằng mili giây.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "LlmMode": {
      "description": "Chế độ gọi mô hình (`§P6.7`).",
      "oneOf": [
        {
          "description": "Gọi thật.",
          "enum": [
            "LIVE"
          ],
          "type": "string"
        },
        {
          "description": "Gọi thật và ghi lại để phát lại sau.",
          "enum": [
            "RECORD"
          ],
          "type": "string"
        },
        {
          "description": "Phát lại từ bản ghi; không có mạng.",
          "enum": [
            "REPLAY"
          ],
          "type": "string"
        },
        {
          "description": "Trả lời cố định. Toàn bộ Giai đoạn B chạy ở chế độ này.",
          "enum": [
            "STUB"
          ],
          "type": "string"
        }
      ]
    },
    "LogFormat": {
      "description": "Định dạng log.",
      "oneOf": [
        {
          "description": "Đọc được bằng mắt, cho lúc phát triển.",
          "enum": [
            "pretty"
          ],
          "type": "string"
        },
        {
          "description": "JSON, cho lúc thu thập.",
          "enum": [
            "json"
          ],
          "type": "string"
        }
      ]
    },
    "ObservabilityConfig": {
      "additionalProperties": false,
      "description": "Quan sát.",
      "properties": {
        "log_format": {
          "allOf": [
            {
              "$ref": "#/definitions/LogFormat"
            }
          ],
          "default": "pretty",
          "description": "Định dạng."
        },
        "log_level": {
          "default": "info",
          "description": "Mức log.",
          "type": "string"
        },
        "otel_endpoint": {
          "default": "",
          "description": "Điểm nhận OTLP. Rỗng là tắt trace.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "PersistenceConfig": {
      "additionalProperties": false,
      "description": "Cấu hình lưu trữ.",
      "properties": {
        "url": {
          "default": "saves/world.sqlite",
          "description": "Đường dẫn file save (`SQLite`) hoặc DSN (Postgres, từ Giai đoạn C).",
          "type": "string"
        }
      },
      "type": "object"
    },
    "SimConfig": {
      "additionalProperties": false,
      "description": "Cấu hình mô phỏng.\n\n**Mọi trường ở đây ảnh hưởng kết quả mô phỏng**, nên đổi chúng giữa chừng phải ghi vào event log (`§P6.1`, `§8.4`) — nếu không replay sẽ lệch mà không có gì trong lịch sử giải thích tại sao.",
      "properties": {
        "active_radius": {
          "default": 3,
          "description": "Bán kính chunk giữ ở mức `active`, tính từ tiêu điểm.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "chunk_size": {
          "default": 32,
          "description": "Cạnh của một chunk, tính bằng ô.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "snapshot_interval": {
          "default": 10000,
          "description": "Số tick giữa hai lần chụp ảnh tự động. `0` là tắt.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "tick_rate": {
          "default": 20,
          "description": "Số tick thần mỗi giây thời gian thật khi chạy ở tốc độ ×1.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "VectorConfig": {
      "additionalProperties": false,
      "description": "Cấu hình chỉ mục ký ức.",
      "properties": {
        "dimension": {
          "default": 768,
          "description": "Số chiều của embedding.",
          "format": "uint",
          "minimum": 0.0,
          "type": "integer"
        },
        "url": {
          "default": "memory-index/index.sqlite",
          "description": "Đường dẫn file chỉ mục.\n\n**Phải khác** [`PersistenceConfig::url`] — `P0-09`. Kho ký ức nằm ở file riêng để không tranh khóa với tiến trình mô phỏng, và để `PC-06` xóa sạch chỉ mục mà không đụng vào save của người chơi.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  "description": "Toàn bộ cấu hình ứng dụng.",
  "properties": {
    "budget": {
      "allOf": [
        {
          "$ref": "#/definitions/BudgetConfig"
        }
      ],
      "default": {
        "max_calls_per_tick": 8,
        "max_concurrent": 4
      },
      "description": "Ngân sách nhận thức."
    },
    "content": {
      "allOf": [
        {
          "$ref": "#/definitions/ContentConfig"
        }
      ],
      "default": {
        "packs": [
          "core"
        ],
        "root": "content"
      },
      "description": "Content pack."
    },
    "embedding": {
      "allOf": [
        {
          "$ref": "#/definitions/EmbeddingConfig"
        }
      ],
      "default": {
        "api_key_env": "EMBEDDINGS_API_KEY",
        "base_url": "",
        "batch_size": 32,
        "document_prefix": "",
        "mode": "STUB",
        "model": "",
        "provider": "",
        "query_prefix": "",
        "send_dimensions": true,
        "timeout_ms": 30000
      },
      "description": "Sinh vector cho chỉ mục ký ức."
    },
    "env": {
      "default": "dev",
      "description": "Tên môi trường đã thật sự được nạp.\n\n**Dẫn xuất, không phải cấu hình.** [`crate::load`] ghi đè trường này bằng tên môi trường nó vừa nạp, nên giá trị trong YAML chỉ có tác dụng làm tài liệu và `MOW_ENV` không sửa được nó. Xem `load` để biết vì sao.",
      "type": "string"
    },
    "llm": {
      "allOf": [
        {
          "$ref": "#/definitions/LlmConfig"
        }
      ],
      "default": {
        "api_key_env": "OPENROUTER_API_KEY",
        "app_title": "",
        "app_url": "",
        "base_url": "",
        "cassette_dir": "llm-cassettes",
        "cognitive_latency_ticks": 10,
        "max_output_tokens": 1024,
        "mode": "STUB",
        "model": "",
        "provider": "",
        "temperature_milli": 0,
        "timeout_ms": 30000
      },
      "description": "Mô hình ngôn ngữ."
    },
    "observability": {
      "allOf": [
        {
          "$ref": "#/definitions/ObservabilityConfig"
        }
      ],
      "default": {
        "log_format": "pretty",
        "log_level": "info",
        "otel_endpoint": ""
      },
      "description": "Quan sát và log."
    },
    "persistence": {
      "allOf": [
        {
          "$ref": "#/definitions/PersistenceConfig"
        }
      ],
      "default": {
        "url": "saves/world.sqlite"
      },
      "description": "Lưu trữ."
    },
    "sim": {
      "allOf": [
        {
          "$ref": "#/definitions/SimConfig"
        }
      ],
      "default": {
        "active_radius": 3,
        "chunk_size": 32,
        "snapshot_interval": 10000,
        "tick_rate": 20
      },
      "description": "Mô phỏng."
    },
    "vector": {
      "allOf": [
        {
          "$ref": "#/definitions/VectorConfig"
        }
      ],
      "default": {
        "dimension": 768,
        "url": "memory-index/index.sqlite"
      },
      "description": "Chỉ mục ký ức."
    }
  },
  "title": "AppConfig",
  "type": "object"
}
